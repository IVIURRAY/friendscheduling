[variables]
JAVA_VERSION = "21"

[phases.setup]
nixPkgs = ["openjdk21", "curl"]

[phases.build]
cmd = "chmod +x ./build-backend.sh && ./build-backend.sh"

[start]
cmd = "cd backend && java -Dserver.port=$PORT -jar build/libs/demo-0.0.1-SNAPSHOT.jar"